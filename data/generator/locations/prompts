pliki które tworzysz zawierać mają strukturę która jest łatwa do odczytania dla agenta ai. 

1. w pliku countries.agent stwórz liste 20 popularnych krajów dla turystów

2. w pliku cities.json wygeneruj listę (4-10) atrakcyjnych dla turystów miast dla każdego z krajów z pliku countries.agent
zróżnicój miasta pod turystów zainteresowanych różnymi rodzajami wypoczynku, takimi jak:
- ciszy i spokoju, 
- wielu atrakcji i zabawy,
- historii i zwiedzania

dane wyjściowe mają zawerać tylko liste krajów z podlistą miast i regionu gdzie leży miasto zgodnie z kontraktem w pliku cities.contract

3. stwórz w pliku country-generator.py skrypt który dla każdego kraju w pliku cities.json stworzy plik o nazwie kraju i wygeneruje w nim elementy json zgodny z kontraktem w locations.contract i wypełni odpowiadające pole location danymi z cities.json
pozostałe pola pozostaw z wartością null, wstaw 3 elemnty attraction i pozostaw 12 obiektów w conditionsInMonth

5. wygeneruj 3 atrakcje w mieście Telč w czechach z informacją o cenie, nazwie i krótkim opisie i polem images z pustą tablicą, następnie wygeneruj liste z średnią temperaturąoraz ilością turystów w skali 1-10 dla każdemo miesiaca - wygeneruj z tego obiekt json w formacie zgodnym z city.contract

{ "attractions": [ { "name": nazwa, "description": opis, "price": cena / 0, "images": [] }, ... ],
"conditionsInMonth": { "1": { "density": null, "temp": null }, 
... "12": { "density": null, "temp": null } } }


6. Przeanalizuj strukture json w locations.json i napisz skrypt w addImages.py któy będzie losował dla każdego elementu attraction w każdym mieście dwa obrazki z tablicy stringów z url (dodaj dwa example url do tej tablicy)

