pliki które tworzysz zawierać mają strukturę która jest łatwa do odczytania dla agenta ai. 

1. Przeanalizuj plik transport/locations.json i stwórz skrypt python w pliku generate-timetable.py który wylosuje n razy pare miast (guid) i dla każdej pary  wylosuje 3 godziny odjazdu, dla każdej godziny odjazdu wylosuje liczbe liczbe minut między 50-500 ile trwa podróż i wygeneruje timestampy dla wyjazdu i dotarcia do celu dla każdego odjazdu w wylosowanym rozkładzie - na najbliższe 60dni (60 powtófrzeń) - i zapisze efekt na liście json w pliku transport.json w formacie zgodnym z kontraktem któy możesz przeanalizować w transport.contract - wartość pola type powinien losować w stosunku 1-5 między "flight" a "train" a nazwe przewoźnika "name" powinien losować w odpowiedniej tablicy stringów jednej dla flight i osobnej dla train (będą tam nazwy przwoźnikó w stylu rayanair/intercity itp)


