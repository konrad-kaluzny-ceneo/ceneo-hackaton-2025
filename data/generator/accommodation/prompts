pliki które tworzysz zawierać mają strukturę która jest łatwa do odczytania dla agenta ai. 

1. pracujemy na plikach w data/generator/accomodation. przeanalizuj strukture json pliku locations.json , stwórz skrypt w pliku generarate.py, który dla każdego miasta w locations doda wpisy dla każdego hotelu w accommodations.json zgodnie z accomodation.contract linkując locationid z locations i generując dla każdego hotelu w danym mieście unikalny id. następnie dla każdego hotelu w każdym mieście dodaj wpis w pliku accommodation-desc.json który połączy opisy attractions w danym mieście oraz opis danego hotelu w jeden string "description" dla każdego hotelu id (plik zawiera liste [{hotelId:..., description:...},...]) 
